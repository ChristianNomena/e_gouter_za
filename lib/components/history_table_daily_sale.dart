import 'package:flutter/material.dart';

class HistoryTableDailySale extends StatelessWidget {
  const HistoryTableDailySale({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
